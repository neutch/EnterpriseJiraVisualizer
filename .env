# Jira Configuration
JIRA_BASE_URL=
JIRA_EMAIL=
JIRA_API_TOKEN=

# JQL Filter - customize to match your project structure
JQL_FILTER=project in (ABC,XYZ) AND issuetype in (Feature,Epic,Story)

# Server Configuration
PORT=4000
NODE_ENV=development

# Client Configuration  
CLIENT_URL=http://localhost:3000

# Optional: Redis for caching (future enhancement)
# REDIS_URL=redis://localhost:6379

# Optional: Database connection (future enhancement)  
# DATABASE_URL=postgresql://user:password@localhost:5432/jira_visualizer
